{% extends 'backbase.html.twig' %}

{% block title %}Edit Livraison{% endblock %}

{% block body %}
    {{ form_start(form) }}
    {{ form_row(form.Progres, {'attr': {'class': 'form-control form-control-user'}}) }}
    <button type="submit" class="btn btn-primary" id="button-to-bag">Update</button>
    {{ form_end(form) }}
{% endblock %}

