<form method="post" action="{{ path('app_offre_delete', {'offreid': offre.offreid}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ offre.offreid) }}">
    <button class="btn">Delete</button>
</form>
