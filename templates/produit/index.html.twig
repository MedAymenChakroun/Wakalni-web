{% extends 'base-front.html.twig' %}
{% block title %}Produit index
{% endblock %}
{% block body %}
	<h1>Produit index</h1>

	{# <table class="table">
	        <thead>
	            <tr>
	                <th>Produitid</th>
	                <th>Nom</th>
	                <th>Type</th>
	                <th>Prix</th>
	                <th>actions</th>
	            </tr>
	        </thead>
	        <tbody>
	        {% for produit in produits %}
	            <tr>
	                <td>{{ produit.produitid }}</td>
	                <td>{{ produit.nom }}</td>
	                <td>{{ produit.type }}</td>
	                <td>{{ produit.prix }}</td>
	                <td>
	                    <a href="{{ path('app_produit_show', {'produitid': produit.produitid}) }}">show</a>
	                    <a href="{{ path('app_produit_edit', {'produitid': produit.produitid}) }}">edit</a>
	                </td>
	            </tr>
	        {% else %}
	            <tr>
	                <td colspan="5">no records found</td>
	            </tr>
	        {% endfor %}
	        </tbody>
	    </table> #}


	<div class="row">
		<div class="col-md-12">
			<p class="text-roboto-light">30 Products in Category
				<strong>Healthy</strong>
			</p>
		</div>

			<div class="caption box">
                {% for produit in produits %}
				<div class="col-sm-4 col-md-4">
					<a href="productDetail.html" class="thumbnail_item thumbnail less-padding less-margin">
						<img src="https://media-cdn.tripadvisor.com/media/photo-s/1a/30/11/62/makloub-our-specialty.jpg" alt="risotto lemon">
					</a>
					<h3>{{ produit.produitid }}</h3>
					<div class="row">
						<div class="col-sm-8 col-xs-6">
							<p class="default-userProductList-CardList-price">{{ produit.nom }}</p>
							<span class="min-order">{{ produit.prix }}</span>
						</div>

						<div class="col-sm-4 col-xs-6">
							<button onclick="location.href='shoppingCart.html'" class="btn default-userProductList-CardList-button pull-right" role="button">test add button</button>
						</div>
					</div>
				</div>
                {% endfor %}
		
		</div>
	</div>

	<a href="{{ path('app_produit_new') }}">Create new</a>
{% endblock %}
