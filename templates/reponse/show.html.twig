{% extends 'base-back.html.twig' %}

{% block title %}Reponse{% endblock %}

{% block body %}
    <h1>Reponse</h1>

    <table class="table">
        <tbody>

            <tr>
                <th>Reponse</th>
                <td>{{ reponse.reponse }}</td>
            </tr>
        </tbody>
    </table>

    {# <button> <a href="{{ path('app_reponse_index') }}">back to list</a></button>#}

    <button> <a href="{{ path('app_reclamation_show',{'reclamationid':reponse.reclamationid.reclamationid}) }}">back to reclamation</a></button>

    <button> <a href="{{ path('app_reponse_edit', {'reponseid': reponse.reponseid}) }}">edit</a></button>

    {{ include('reponse/_delete_form.html.twig') }}
{% endblock %}
