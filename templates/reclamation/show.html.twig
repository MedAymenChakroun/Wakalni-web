{% extends 'base-front.html.twig' %}

{% block title %}Reclamation{% endblock %}

{% block body %}
    <h1>Reclamation</h1>

    <table class="table">
        <tbody>

            <tr>
                <th>Sujet</th>
                <td>{{ reclamation.sujet }}</td>
            </tr>
            <tr>
                <th>Etat</th>
                <td>{{ reclamation.etat }}</td>
            </tr>
            <tr>
                <th>Contenu</th>
                <td>{{ reclamation.contenu }}</td>
            </tr>
            <tr>
                <th>Reponse</th>
                <td>{{ reponse.reponse }}</td>
            </tr>
        </tbody>
    </table>

<button ><a href="{{ path('app_reclamation_index') }}">back to list</a>
</button>
   <button> <a href="{{ path('app_reclamation_edit', {'reclamationid': reclamation.reclamationid}) }}">edit</a>
   </button>
    {{ include('reclamation/_delete_form.html.twig') }}

{# <a href="{{ path('app_reponse_showbyid',{ 'id': reclamation.reclamationid }) }}">CHECK</a> #}
{#    <a href="{{ path('app_reponse_newV',{ 'idR': reclamation.reclamationid }) }}">Affecter une réponse</a>#}
{% endblock %}
