{% extends 'base-back.html.twig' %}

{% block title %}Leftover{% endblock %}

{% block body %}
    <h1>Leftovers</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Leftoverid</th>
                <td>{{ leftover.leftoverid }}</td>
            </tr>
            <tr>
                <th>Sujet</th>
                <td>{{ leftover.sujet }}</td>
            </tr>
            <tr>
                <th>Type</th>
                <td>{{ leftover.type }}</td>
            </tr>
            <tr>
                <th>Quantite</th>
                <td>{{ leftover.quantite }}</td>
            </tr>
            <tr>
                <th>Dateexpiration</th>
                <td>{{ leftover.dateexpiration ? leftover.dateexpiration|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_leftovers_index') }}">back to list</a>

    <a href="{{ path('app_leftovers_edit', {'leftoverid': leftover.leftoverid}) }}">edit</a>

    {{ include('leftovers/_delete_form.html.twig') }}
{% endblock %}
